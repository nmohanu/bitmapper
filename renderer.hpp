#include "SFML/Graphics.hpp"
#include "camera.hpp"
#include "map.hpp"
#include "program_state.hpp"

void render_map(sf::RenderWindow& w, ProgramState& state);

void draw_grid(sf::RenderWindow& w, ProgramState& state);
